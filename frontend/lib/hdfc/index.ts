/**
 * HDFC client-specific lib.
 * Add HDFC data overrides or API helpers here when needed.
 * Pages use shared @/lib (api, voiceData, compliance, fci-lib, vendor, unified) by default.
 */

export const HDFC_CLIENT_ID = 'hdfc';
